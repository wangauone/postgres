name: Plugin sync

on:
  pull_request:
    paths:
      - plugin.json
      - mcp.json

permissions:
  contents: write

jobs:
  sync:
    uses: wangauone/agent-plugin-sync/.github/workflows/sync.yml@main
    with:
      auto_sync: true
