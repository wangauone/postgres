{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "postgresql": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@toolbox-sdk/server@1.7.0",
        "--prebuilt",
        "postgres",
        "--stdio"
      ]
    }
  }
}
